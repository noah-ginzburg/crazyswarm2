# named list of all robots
fileversion: 3

robots:
  # current starting positions:
  #
  #        ^ x
  #     <--+
  #        y
  #
  #  0.4  0.0 -0.4 pos y/
  #  -------------- / pos x
  #   5    4    3  |  0.4
  #   6    1    2  |  0.0
  #   7    8    9  | -0.4
  #  
  #  in meter



  # cf02:
  #   #enabled: true
  #   enabled: false
  #   uri: radio://0/80/2M/E7E7E7E702
  #   initial_position: [ 0.0, 0.4, 0.0]
  #   type: cf21

  cf03:
    enabled: false
    # enabled: false
    uri: radio://0/100/2M/E7E7E7E703
    initial_position: [-0.5, 1.0, 0.0]
    type: cf21

  cf02:
    # enabled: true
    enabled: false
    uri: radio://0/100/2M/E7E7E7E702
    initial_position: [0.5, 1.0, 0.0]
    type: cf21

  cf09: # das hatte heute schon funktioniert
    # enabled: true
    enabled: true
    uri: radio://0/100/2M/E7E7E7E7E9
    initial_position: [ -0.478, -0.47, 0.0]
    type: cf21

  cf01:
    enabled: false
    # enabled: false
    uri: radio://0/100/2M/E7E7E7E701
    initial_position: [ 0.0, 0.0, 0.0]
    type: cf21  # see robot_types


  # cf10: # das hatte heute schon funktioniert
  #   enabled: true
  #   # enabled: false
  #   uri: radio://0/80/2M/E7E7E7E7EA
  #   initial_position: [ 0.4, 0.4, 0.0]
  #   type: cf21
  # cf11: # das hatte heute schon funktioniert
  #   enabled: true
  #   # enabled: false
  #   uri: radio://0/80/2M/E7E7E7E7EB
  #   initial_position: [ 0.4, -0.4, 0.0]
  #   type: cf21
  # cf12: # das hatte heute schon funktioniert
  #   enabled: true
  #   # enabled: false
  #   uri: radio://0/80/2M/E7E7E7E7EC
  #   initial_position: [ -0.4, -0.4, 0.0]
  #   type: cf21
  # cf13: # das hatte heute schon funktioniert
  #   enabled: true
  #   # enabled: false
  #   uri: radio://0/80/2M/E7E7E7E7ED
  #   initial_position: [ -0.4, 0.4, 0.0]
  #   type: cf21


  # The following robots are waiting for new markers
  # cf07: 
    # enabled: true
    # enabled: false
  #   uri: radio://0/80/2M/E7E7E7E707
  #   initial_position: [-0.4, 0.4, 0.0]
  #   type: cf21
    
  # cf08:
    # enabled: true
    # enabled: false
  #   uri: radio://0/80/2M/E7E7E7E7E7
  #   initial_position: [-0.4, 0.0, 0.0]
  #   type: cf21

  # cf09:
  #   enabled: false
  #   # enabled: true
  #   uri: radio://0/80/2M/E7E7E7E7E7
  #   initial_position: [-0.4,-0.4, 0.0]
  #   type: cf21

# Definition of the various robot types

robot_types:
  cf21:
    motion_capture:
      # tracking: "vendor" # one of "vendor", "librigidbodytracker"
      tracking: "librigidbodytracker" # one of "vendor", "librigidbodytracker"
      # only if enabled; see motion_capture.yaml
      # marker: default_single_marker
      marker: arrangement_v3
      dynamics: default
    big_quad: false
    battery:
      voltage_warning: 3.8  # V
      voltage_critical: 3.7 # V
    # firmware_params:
    #   kalman:
    #     pNAcc_xy: 1.0 # default 0.5
    # firmware_logging: DISABLED -- every log topic here is extra radio traffic
    # competing with the extpos/extpose position corrections sent to the drone;
    # this custom logging was fighting the Vicon corrections for radio time and
    # causing the estimator to stall/drift. Re-enable only for debugging, not
    # during normal flight.
    # firmware_logging:
    #   enabled: true
    #   custom_topics:
    #     low_level_control:
    #       frequency: 50
    #       vars: ["ctrlMel.cmd_thrust", "ctrlMel.cmd_roll", "ctrlMel.cmd_pitch", "ctrlMel.cmd_yaw"]
    #     # Per-motor PWM (0..65535). A weak motor shows consistently HIGHER pwm than
    #     # its diagonal partner; pegged at 65535 = saturated -> no attitude headroom.
    #     motor_pwm:
    #       frequency: 20
    #       vars: ["motor.m1", "motor.m2", "motor.m3", "motor.m4"]

    # reference_frame: "odom" 

  cf21_mocap_deck:
    motion_capture:
      tracking: "librigidbodytracker" # one of "vendor", "librigidbodytracker"
      # only if enabled; see motion_capture.yaml
      marker: mocap_deck
      dynamics: default
    big_quad: false
    battery:
      voltage_warning: 3.8  # V
      voltage_critical: 3.7 # V
    # firmware_params:
    #   kalman:
    #     pNAcc_xy: 1.0 # default 0.5
    #firmware_logging:
    #   enabled: true
    #   default_topics:
    #   pose:
    #     frequency: 1 # Hz
    #   custom_topics:
    #    topic_name3: 
    #      frequency: 1
    #      vars: ["acc.x", "acc.y", "acc.z"]
    # reference_frame: "mocap" 

# global settings for all robots
all:
  # firmware logging for all drones (use robot_types/type_name to set per type, or
  # robots/drone_name to set per drone)
  # firmware_logging: DISABLED -- pose (100Hz) and status (1Hz) readback were
  # competing with position-correction packets for radio time (see note in
  # robot_types.cf21). Re-enable only for debugging, not during normal flight.
  firmware_logging:
    enabled: true
    default_topics:
      pose:
        frequency: 2 # Hz
      status:
        frequency: 1 # Hz
    # --- tuning diagnostics (uncomment to re-enable; adds radio load) ---
    custom_topics:
    #   ctrl:
    #     frequency: 50 # Hz
    #     # What the position/velocity controller is COMMANDING the attitude loop to do.
    #     # On a pure vertical (z) command these should stay near 0; if roll/pitch
    #     # targets swing, the position controller is over-reacting -> wobble/flip.
    #     vars: ["ctrltarget.roll", "ctrltarget.pitch", "stabilizer.thrust"]
      # Per-motor PWM (0..65535) to find a weak motor/prop: the weak one runs
      # consistently HIGHER than its diagonal partner; pegged at 65535 = saturated.
      # TEMPORARY debug load -- remove again after the asymmetry is found.
      motor_pwm:
        frequency: 25 # keep low: downlink log traffic competes with 100Hz extpose uplink
        vars: ["motor.m1", "motor.m2", "motor.m3", "motor.m4"]
      est:
        frequency: 5 # Hz
        # Kalman position variance = estimate health. If these are large or growing,
        # the estimate is bad -> fix Vicon/extpos before touching any gain.
        # roll/pitch/yaw added to diagnose spiral-on-takeoff: position topics can
        # all "match" while the mocap object frame is yawed/tilted vs the body.
        vars: ["stateEstimate.x", "stateEstimate.y", "stateEstimate.z",
               "stateEstimate.roll", "stateEstimate.pitch", "stateEstimate.yaw"]
  # firmware parameters for all drones (use robot_types/type_name to set per type, or
  # robots/drone_name to set per drone)
  firmware_params:
    commander:
      enHighLevel: 1
    stabilizer:
      # estimator: 1 # 1: complementary, 2: kalman
      estimator: 2 # 1: complementary, 2: kalman
      controller: 1 # 1: PID, 2: mellinger  (PID for bring-up; mellinger flips on imperfect estimate)
      # controller: 2 # 1: PID, 2: mellinger
    # ring:
    #   effect: 16 # 6: double spinner, 7: solid color, 16: packetRate
    #   solidBlue: 255 # if set to solid color
    #   solidGreen: 0 # if set to solid color
    #   solidRed: 0 # if set to solid color
    #   headlightEnable: 0
    locSrv:
      extPosStdDev: 1e-2
      extQuatStdDev: 4.5e-3
    kalman:
      resetEstimation: 1   # clear NaN/stale estimator state on connect (NaN never self-heals)
  # reference frame for on-board state-estimate of all drones (use robot_types/type_name to set per type, or
  # robots/drone_name to set per drone)
  # reference_frame: "world"
  reference_frame: "mocap"
  broadcasts:
    num_repeats: 15 # number of times broadcast commands are repeated
    delay_between_repeats_ms: 1 # delay in milliseconds between individual repeats
